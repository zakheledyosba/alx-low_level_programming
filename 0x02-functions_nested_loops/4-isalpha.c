#include "main.h"


/**
 * main - Write a function that checks for alphabetic character. 
 * @c: is the char to be checked
 * Return: 0
 */

int _isalpha(int c)

{

	if((c >= 'a' && c <= 'z') ||

	  ((c >= 'A' && c <= 'Z'))

	  	return (1);

	else

		return (0);

}
